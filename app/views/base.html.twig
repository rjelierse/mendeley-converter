<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Convert your Mendeley file</title>
    <base href="/mendeley/">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="css/bootstrap-responsive.min.css" rel="stylesheet">

    <!--[if lt IE 9]>
    <script src="js/html5shiv.js"></script>
    <![endif]-->
</head>

<body>
<div class="container">
    {% block content %}
    <div class="row">
        <div class="span12">
            <p class="alert alert-{{ message_type | default('info') }}">{{ message | default('No message set') }}</p>
        </div>
    </div>
    {% if trace is defined %}
    <div class="row">
        <div class="span12">
            <h3>Trace:</h3>
            <div class="well">
                <ol>
                    {% for frame in trace %}
                        <li>Call to <strong>{{ frame.function }}</strong> in <strong>{{ frame.file }}</strong> on line <strong>{{ frame.line }}</strong></li>
                    {% else %}
                        <li>No backtrace available.</li>
                    {% endfor %}
                </ol>
            </div>
        </div>
    </div>
    {% endif %}
    {% endblock %}
</div>

<script src="js/jquery.min.js"></script>
<script src="js/bootstrap.min.js"></script>

</body>
</html>
